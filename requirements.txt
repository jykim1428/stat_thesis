# 8월 1주차: FinRL 환경 세팅 (PortfolioOptimizationEnv, PPO 베이스라인용)
# 로컬(python -m venv .venv)과 Colab 양쪽에서 이 파일로 재현되는지 확인할 것.
#
# 비고:
# - PortfolioOptimizationEnv는 finrl_env/에 벤더링(원본 pip finrl 패키지는
#   관련없는 Alpaca 페이퍼트레이딩 의존성까지 강제로 물고 와서 배제함).
#   출처: finrl_env/env_portfolio_optimization.py 상단 주석 참고.
# - gym(구식, 0.26.x)은 PortfolioOptimizationEnv가 직접 요구해서 유지.
#   NumPy 2.0 비호환 경고가 뜨지만 reset()/step() 동작은 확인됨.

torch==2.13.0
stable-baselines3==2.9.0
gym==0.26.2
gymnasium==1.3.0
shimmy==2.0.1

pandas==3.0.5
pandas-ta==0.4.71b0
numpy==2.2.6
pyarrow==25.0.0

matplotlib==3.11.1
seaborn==0.13.2
quantstats==0.0.81

python-binance==1.0.19
python-dotenv==1.1.1
