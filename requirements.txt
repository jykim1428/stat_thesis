# 8월 1주차: FinRL 환경 세팅 (PortfolioOptimizationEnv, PPO 베이스라인용)
# 로컬(python -m venv .venv)과 Colab 양쪽에서 이 파일로 재현되는지 확인할 것.
#
# 비고:
# - PortfolioOptimizationEnv는 src/cryptoagent/envs/에 벤더링(원본 pip finrl 패키지는
#   관련없는 Alpaca 페이퍼트레이딩 의존성까지 강제로 물고 와서 배제함).
#   출처: src/cryptoagent/envs/env_portfolio_optimization.py 상단 주석 참고.
# - gym(구식, 0.26.x)은 PortfolioOptimizationEnv가 직접 요구해서 유지.
#   NumPy 2.0 비호환 경고가 뜨지만 reset()/step() 동작은 확인됨.
# - pip install -e . 을 함께 실행해야 cryptoagent 패키지를 어디서든 import 가능 (pyproject.toml 참고).

torch==2.13.0
stable-baselines3==2.9.0
gym==0.26.2
gymnasium==1.3.0
shimmy==2.0.1

pandas==3.0.5
pandas-ta==0.4.71b0
numpy==2.2.6
pyarrow==25.0.0

matplotlib==3.11.1
seaborn==0.13.2
quantstats==0.0.81

python-binance==1.0.19
python-dotenv==1.1.1

# 8월 2주차: W&B 연동 (train_ppo_mlp.py의 tensorboard_log/callback 파라미터 의존성)
wandb==0.28.1
tensorboard==2.21.0

# 8월 4주차: 전통 벤치마크 (Markowitz, Risk Parity ERC) 의존성
scipy==1.16.3
PyPortfolioOpt==1.6.0

# 8월 2~3주차: 공용 평가 모듈 (evaluate.py 의존성)
# 원본 PyPI의 `empyrical`은 Python 3.13에서 설치 자체가 실패함
# (configparser.SafeConfigParser 제거로 인한 빌드 에러). 같은 empyrical
# 네임스페이스로 import되는 유지보수 포크를 대신 씀 - 코드는 무수정.
empyrical-reloaded==0.5.12
